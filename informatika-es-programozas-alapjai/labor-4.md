# Negyedik labor

```cpp
#include <locale.h> // setlocale éáö
#include <stdio.h>
#define _USE_MATH_DEFINES
#include <math.h>

int main(void)
{
    int c=1, d, e, j, i, x;
    double y, y1, x1, x2, xko;
    const double eps=1e-10;
    setlocale(LC_ALL, "hu"); // magyar terület, scanf, printf-ben tizedesvesszo lesz pont helyett
    // ő és ű nincs
    y = 1.5;

    while (i>=0) printf("%i", x & (1 << i--) ? 1 : 0);

    return 0;
}

```